name: Debug Agent (on push)

on:
  push:
    branches: [main, staging]
  pull_request:
    branches: [main, staging]

jobs:
  debug:
    uses: obrandrup/debug-agent-central/.github/workflows/debug-agent-reusable.yml@main
    secrets:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
